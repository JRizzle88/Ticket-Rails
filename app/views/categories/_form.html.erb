<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div class="alert alert-error">
      <b><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</b>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="form-actions">
    <%= f.submit :class => "btn btn-success" %><br />
    <%= link_to 'Cancel', categories_path %>
  </div>
<% end %>
