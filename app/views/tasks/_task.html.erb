
  <div class="list-group-item">
      <div class="project-task" id="task-id-<%= task.id %>">

        

          <%= task.title %>
          <%= link_to "", project_task_path(task.project, task), :method => :delete, data: { :confirm => "Are you sure you want to delete this task?" },
                        :class => 'glyphicon glyphicon-trash pull-right', remote: true,
                        title: 'Delete task', rel: 'tooltip' %>

      </div>
  </div>
