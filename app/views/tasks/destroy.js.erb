$('#tasks-list').html('<%= escape_javascript(render partial: @project.tasks) %>');
$('#tasks').html('This article has <%= escape_javascript(pluralize(@project.tasks.count, 'task')) %>');
$('#task_body.form-control').val('');
